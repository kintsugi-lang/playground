{
  "name": "kintsugi-playground",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync": "./sync-bundle.sh",
    "dev": "npm run sync && vite",
    "build": "npm run sync && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.7.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@codemirror/commands": "^6.10.3",
    "@codemirror/language": "^6.12.3",
    "@codemirror/state": "^6.6.0",
    "@codemirror/view": "^6.41.0"
  }
}
